# Changes

> ℹ️
>
> I update the align.py script to not include a `/` after the `--outprefixname` flag. In additon I lowered the resources assigned to the align rule, as to fix the STAR file descriptor limit issues.
